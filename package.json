{
  "name": "eco-nera",
  "version": "0.1.0",
  "description": "Sparatutto coop al buio per due, server sul PC e client Android.",
  "private": true,
  "type": "module",
  "scripts": {
    "server": "node server/server.js",
    "sync": "cap sync android",
    "apk": "cap sync android && cd android && gradlew.bat assembleDebug",
    "apri-android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
